# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
websockets>=13.0
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# Azure
azure-identity>=1.19
azure-storage-blob>=12.24

# LLM
anthropic>=0.40

# ML / embeddings / clustering
# torch/sentence-transformers pinned to match the pre-fine-tuned models'
# saved environment (see models/*/config_sentence_transformers.json)
torch==2.12.0
sentence-transformers==5.5.1
transformers==5.10.1
scipy>=1.14
numpy>=2.0
scikit-learn>=1.5
# Some fine-tuned models ship as LoRA adapters rather than merged weights;
# scripts/prepare_embedding_models.py merges them into the base model at install
# time so inference pays no adapter overhead.
peft>=0.17

# Data
pandas>=2.2
openpyxl>=3.1

# Document parsing
pdfplumber>=0.11
pypdf>=5.0
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.3
charset-normalizer>=3.4
# repairs double-encoded text (valid UTF-8 that literally contains "Â£") — the
# real sample client JDs need this; see services/ingestion/parsers.py
ftfy>=6.3

# Export rendering
weasyprint>=63.0
jinja2>=3.1

# Utilities
tenacity>=9.0

# Test/verification scripts only. `websockets` above is the server side; this is
# the blocking client the end-to-end scripts use to follow a job's progress the
# same way the browser does.
requests>=2.32
websocket-client>=1.8
